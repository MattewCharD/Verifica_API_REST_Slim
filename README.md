## Comandi
`git clone -b verifica_1 https://github.com/benve-meucci/lamp-slim-react.git`
`cd lamp-slim-react`
`git branch -m master`
`git remote remove origin`

## Su Linux
`MY_UID=$(id -u) MY_GID=$(id -g) docker-compose up`

## Su Windows
`docker-compose up`
